#actuator是否需要安全保证
management.security.enabled=false

eureka.client.serviceUrl.defaultZone=${eureka.url:http://localhost:8761/eureka/}
#eureka.client.serviceUrl.defaultZone=${eureka.url:http://localhost:8761/eureka/}
eureka.instance.prefer-ip-address=true

spring.cloud.config.discovery.enabled=true
spring.cloud.config.discovery.serviceId=SERVER-CONFIG
spring.cloud.config.name=port,db,mail,redis,mq
spring.cloud.config.profile=${config.profile:dev}
server.port=${ops.alert.port:9008}