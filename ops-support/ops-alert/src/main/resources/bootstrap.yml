spring:
  cloud:
    config:
      discovery:
        serviceId: SERVER-CONFIG
        enabled: true
      profile: ${config.profile:dev}
      name: port,db,mail,redis,mq
  application:
    name: ops-alert
server:
  port: ${ops.alert.port:9008}
eureka:
  instance:
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: ${eureka.url:http://10.180.8.208:31057/eureka/}
